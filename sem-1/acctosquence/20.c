#include <stdio.h> 
#include <limits.h>


void main(){
    int min, max;

    min = INT_MIN;
    max = INT_MAX;

    printf("Minimum value of integer is: %d\n", min);
    printf("Maximum value of integer is: %d\n", max);
}